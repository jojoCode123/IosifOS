#pragma once

#include "util/types.hpp"

#define ADD(x, y) (x + y)
#define SUB(x, y) (x - y)
#define MUL(x, y) (x * y)
#define DIV(x, y) (x / y)
#define MOD(x, y) (x % y)

#define SHL(x, y) (x << y)
#define SHR(x, y) (x >> y)